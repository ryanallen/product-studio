---
name: install
description: Run the standard Product Studio install steps. Use when user says "setup", "install", or /install. In Cursor, Claude Code, and other clients, /skills lists all.
---

# Install

Run the **Install** workflow in [Coordinator](../agents/coordinator.md): install-config → install-choices → install-mcp → install-figma-bridge (if figma-console) → install-handoff.

Step details live in the skills for each step: [install-config](../install-config/SKILL.md), [install-choices](../install-choices/SKILL.md), [install-mcp](../install-mcp/SKILL.md), [install-figma-bridge](../install-figma-bridge/SKILL.md), [install-handoff](../install-handoff/SKILL.md).
