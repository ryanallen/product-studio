# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# npm
node_modules/
package-lock.json

# Local paths config (copy from work/paths.md.example)
work/paths.md

# Install handoff (agent-created, do not commit)
.claude/skills/install/install-handoff.marker

# Figma bridge (created by npm run setup:figma-bridge)
.claude/skills/generate-figma/scripts/figma-desktop-bridge/
